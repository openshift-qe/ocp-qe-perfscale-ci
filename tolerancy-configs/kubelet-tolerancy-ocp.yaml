- json_path: ["metricName", "kubeletCPU", "jobConfig.name", "*", "max(value)"]
  tolerancy: -15
  # Maximum percentage of allowed failures
  max_failures: 100
- json_path: ["metricName", "kubeletMemory", "jobConfig.name", "*", "max(value)"]
  tolerancy: -15
  # Maximum percentage of allowed failures
  max_failures: 100