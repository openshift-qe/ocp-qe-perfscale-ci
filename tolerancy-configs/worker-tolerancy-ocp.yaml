- json_path: ["metricName", "nodeCPU-Workers","labels.mode","user","max(value)"]
  tolerancy: -15
  # Maximum percentage of allowed failures
  max_failures: 50
- json_path: ["metricName", "nodeCPU-Workers","labels.mode","user","avg(value)"]
  tolerancy: -15
  # Maximum percentage of allowed failures
  max_failures: 50
- json_path: ["metricName", "nodeMemoryUtilization-Workers","jobConfig.name","*","max(value)"]
  tolerancy: -15
  # Maximum percentage of allowed failures
  max_failures: 50
- json_path: ["metricName", "nodeMemoryUtilization-Workers","jobConfig.name","*","avg(value)"]
  tolerancy: -15
  # Maximum percentage of allowed failures
  max_failures: 50