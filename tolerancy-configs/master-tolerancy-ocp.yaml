- json_path: ["metricName", "nodeCPU-Masters", "labels.mode", "users", "max(value)"]
  tolerancy: -15
  # Maximum percentage of allowed failures
  max_failures: 100
- json_path: ["metricName", "nodeMemoryUtilization-Masters", "jobConfig.name", "*", "max(value)"]
  tolerancy: -15
  # Maximum percentage of allowed failures
  max_failures: 100