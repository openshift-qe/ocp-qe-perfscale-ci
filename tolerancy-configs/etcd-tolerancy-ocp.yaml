- json_path: ["metricName", "99thEtcdDiskBackendCommitDurationSeconds", "jobConfig.name","*","max(value)"]
  tolerancy: 40
  # Maximum percentage of allowed failures
  max_failures: 0
- json_path: ["metricName", "99thEtcdDiskWalFsyncDurationSeconds", "jobConfig.name","*","max(value)"]
  tolerancy: 40
  # Maximum percentage of allowed failures
  max_failures: 0
- json_path: ["metricName", "99thEtcdRoundTripTimeSeconds", "jobConfig.name","*","max(value)"]
  tolerancy: 40
  # Maximum percentage of allowed failures
  max_failures: 0
