apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: loki-store
  namespace: network-observability
spec:
  resources:
    requests:
      storage: 200G
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce

